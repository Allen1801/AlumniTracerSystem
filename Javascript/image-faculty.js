var loadFileDean = function(event) {
  var output = document.getElementById('previewdean');
  output.src = URL.createObjectURL(event.target.files[0]);
  output.onload = function(){
    URL.revokeObjectURL(output.src)
  }
};

var loadFileSecretary = function(event) {
  var output = document.getElementById('previewsecretary');
  output.src = URL.createObjectURL(event.target.files[0]);
  output.onload = function(){
    URL.revokeObjectURL(output.src)
  }
};

var loadFileHead1 = function(event) {
  var output = document.getElementById('previewhead1');
  output.src = URL.createObjectURL(event.target.files[0]);
  output.onload = function(){
    URL.revokeObjectURL(output.src)
  }
};

var loadFileHead2 = function(event) {
  var output = document.getElementById('previewhead2');
  output.src = URL.createObjectURL(event.target.files[0]);
  output.onload = function(){
    URL.revokeObjectURL(output.src)
  }
};

var loadFileFT1 = function(event) {
  var output = document.getElementById('previewft1');
  output.src = URL.createObjectURL(event.target.files[0]);
  output.onload = function(){
    URL.revokeObjectURL(output.src)
  }
};var loadFileDean = function(event) {
  var output = document.getElementById('previewdean');
  output.src = URL.createObjectURL(event.target.files[0]);
  output.onload = function(){
    URL.revokeObjectURL(output.src)
  }
};

var loadFileSecretary = function(event) {
  var output = document.getElementById('previewsecretary');
  output.src = URL.createObjectURL(event.target.files[0]);
  output.onload = function(){
    URL.revokeObjectURL(output.src)
  }
};

var loadFileHead1 = function(event) {
  var output = document.getElementById('previewhead1');
  output.src = URL.createObjectURL(event.target.files[0]);
  output.onload = function(){
    URL.revokeObjectURL(output.src)
  }
};

var loadFileHead2 = function(event) {
  var output = document.getElementById('previewhead2');
  output.src = URL.createObjectURL(event.target.files[0]);
  output.onload = function(){
    URL.revokeObjectURL(output.src)
  }
};

var loadFileHead3 = function(event) {
  var output = document.getElementById('previewhead3');
  output.src = URL.createObjectURL(event.target.files[0]);
  output.onload = function(){
    URL.revokeObjectURL(output.src)
  }
};

var loadFileHead4 = function(event) {
  var output = document.getElementById('previewhead4');
  output.src = URL.createObjectURL(event.target.files[0]);
  output.onload = function(){
    URL.revokeObjectURL(output.src)
  }
};

var loadFileFT1 = function(event) {
  var output = document.getElementById('previewft1');
  output.src = URL.createObjectURL(event.target.files[0]);
  output.onload = function(){
    URL.revokeObjectURL(output.src)
  }
};

var loadFileFT2 = function(event) {
  var output = document.getElementById('previewft2');
  output.src = URL.createObjectURL(event.target.files[0]);
  output.onload = function(){
    URL.revokeObjectURL(output.src)
  }
};

var loadFileFT3 = function(event) {
  var output = document.getElementById('previewft3');
  output.src = URL.createObjectURL(event.target.files[0]);
  output.onload = function(){
    URL.revokeObjectURL(output.src)
  }
};

var loadFileFT4 = function(event) {
  var output = document.getElementById('previewft4');
  output.src = URL.createObjectURL(event.target.files[0]);
  output.onload = function(){
    URL.revokeObjectURL(output.src)
  }
};

var loadFileFT5 = function(event) {
  var output = document.getElementById('previewft5');
  output.src = URL.createObjectURL(event.target.files[0]);
  output.onload = function(){
    URL.revokeObjectURL(output.src)
  }
};

var loadFileFT6 = function(event) {
  var output = document.getElementById('previewft6');
  output.src = URL.createObjectURL(event.target.files[0]);
  output.onload = function(){
    URL.revokeObjectURL(output.src)
  }
};

var loadFileFT7 = function(event) {
  var output = document.getElementById('previewft7');
  output.src = URL.createObjectURL(event.target.files[0]);
  output.onload = function(){
    URL.revokeObjectURL(output.src)
  }
};

var loadFileFT8 = function(event) {
  var output = document.getElementById('previewft8');
  output.src = URL.createObjectURL(event.target.files[0]);
  output.onload = function(){
    URL.revokeObjectURL(output.src)
  }
};

var loadFileFT9 = function(event) {
  var output = document.getElementById('previewft9');
  output.src = URL.createObjectURL(event.target.files[0]);
  output.onload = function(){
    URL.revokeObjectURL(output.src)
  }
};
